#include <vector>
#include <string>
#include <iostream>
#include <iomanip>
#include <cstdlib>

class Display{
 public:
  void ShowInfo(int, int);
  void RoundStart(int);
  void Standing(std::vector<struct player>, int);
  void LegWon(std::string);
 private:

  
  
};

